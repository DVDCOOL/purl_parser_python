version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    ports:
      - "8001:5000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1  # Ensure real-time logging